# v99 文字链路板测通过与自动初始化目标

## 实机证据

用户提供的启动及测试记录：`C:/Users/Lenovo/.codex/attachments/0dd5868e-2bb9-4b8e-805d-85453258b422/pasted-text.txt`。此路径是本机附件位置，不是队友环境依赖；原始附件可能含私人配置，不直接复制进公开仓库。

- Wi-Fi、DHCP 成功，官方 AI Agent 使用板端配置连接云端。
- Agent 初始化从 405.444 秒到 405.762 秒，日志标记 ready 为 boot +317 ms。
- 首次请求 TLS 握手从 428.976 秒到 435.988 秒，约 7.012 秒；HTTP first-byte=123 ms，complete=5895 ms，200/chunked；官方记录 llm_ms=12929、status=ok。
- 后续三次官方调用耗时分别为 1224、1279、632 ms，均 status=ok，并复用连接。不能据此宣称长期稳定、所有请求均有该延迟。
- HTTP 收尾不再固定等待约 60 秒，v99 修复已获得此次板测支持。
- Wi-Fi 连接前两轮重扫未发现目标热点，第三轮发现并成功连接：首次联网搜索耗时与首次 TLS/模型调用耗时应分别统计，不混为同一问题。

当前通过的是终端文字闭环，不包含 USB 麦克风、UART TTS、I2S 或多轮会话记忆验收。

## 用户新增要求：联网后自动初始化（待开发）

1. 保留现有 Wi-Fi 自动连接上次网络机制，Agent 不另行重置关联。
2. 开机后台等待 DHCP、配置文件、CA 和可信时间就绪，再启动官方 Agent；不得阻塞 NSH、SSH、FTP。
3. 自动和手动启动共用单实例保护，避免重复创建引擎及线程；提供明确的 starting/ready/failed 状态。
4. 提供可持久保存的自启动开关；未配置 API Key 时仅提示待配置，不循环发送失败请求。
5. 区分 Agent ready 和云端连接 ready，保留可观测的初始化、DNS、TCP、TLS、首字节及完整回复耗时。
6. 断网时采用有上限的退避，不忙轮询，不重复创建实例；恢复网络后复用运行实例。
7. 默认不发送真实 LLM 预热请求，避免静默计费或修改会话。若以后启用，应独立显式选择，并与用户输入序列化。
8. TLS 预连接若实施，复用官方连接池且强校验证书/主机名；连接可能被服务端关闭，不能保证首次用户请求永远免握手。
9. 将改变放在代码仓库 overlay/patch 中，保存恢复快照后构建下一版本，不覆盖已通过的 v99 镜像。

本文件记录要求和板测结论，不表示自动初始化功能已经实现。
