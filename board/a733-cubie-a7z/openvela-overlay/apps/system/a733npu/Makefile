include $(APPDIR)/Make.defs

PROGNAME  = $(CONFIG_A733NPU_PROGNAME)
PRIORITY  = $(CONFIG_A733NPU_PRIORITY)
STACKSIZE = $(CONFIG_A733NPU_STACKSIZE)
MODULE    = $(CONFIG_SYSTEM_A733NPU)
MAINSRC   = a733npu_main.c

include $(APPDIR)/Application.mk
